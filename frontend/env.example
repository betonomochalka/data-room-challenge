# --- Frontend Local .env.local ---

# 1. Your local backend URL
PYTHON_API_URL="http://localhost:3001/api"

# 2. Supabase public keys (safe for frontend)
REACT_APP_SUPABASE_URL="https://[YOUR_SUPABASE_REF].supabase.co"
REACT_APP_SUPABASE_ANON_KEY="[YOUR_SUPABASE_ANON_KEY]" # Public key (anon key)

# 3. Site URL (optional - for OAuth redirects)
# If not set, the app will automatically use window.location.origin (recommended for production)
# Only set this if you need to override the automatic detection
# REACT_APP_SITE_URL="https://your-production-domain.com"